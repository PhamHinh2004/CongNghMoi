<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <title>Edit Collection Piece</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #0f0f0f; color: #fff; }
        .card { background-color: #1a1a1a; border: 1px solid #d4af37; border-radius: 20px; }
        .current-img-box { background: #000; padding: 10px; border-radius: 15px; border: 1px solid #333; }
        .gold-label { color: #d4af37; text-transform: uppercase; font-size: 0.8rem; letter-spacing: 1px; }
        .form-control { background-color: #262626; border: 1px solid #333; color: #fff; }
        .btn-gold { background-color: #d4af37; color: #000; font-weight: bold; }
    </style>
</head>
<body>
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card p-4 shadow-lg">
                <h3 class="text-center mb-4" style="color: #d4af37;">EDIT PRODUCT PIECE [cite: 83]</h3>
                <form action="/edit" method="POST" enctype="multipart/form-data"> [cite: 83]
                    <input type="hidden" name="id" value="<%= product.id %>"> [cite: 84]
                    <input type="hidden" name="old_url_image" value="<%= product.url_image %>"> [cite: 84]

                    <div class="row mb-4">
                        <div class="col-md-4 text-center">
                            <label class="gold-label d-block mb-2">CURRENT [cite: 89]</label>
                            <div class="current-img-box">
                                <img src="<%= product.url_image %>" class="img-fluid rounded" style="max-height: 120px;"> [cite: 89]
                            </div>
                        </div>
                        <div class="col-md-8">
                            <label class="gold-label">REPLACE MASTERPIECE [cite: 90]</label>
                            <input type="file" name="image" class="form-control mb-2"> [cite: 90]
                            <small class="text-muted italic" style="font-size: 0.7rem;">Leave empty to keep existing image</small> [cite: 90]
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="gold-label">NAME [cite: 85]</label>
                        <input type="text" name="name" class="form-control" value="<%= product.name %>" required> [cite: 85]
                    </div>
                    <div class="row mb-4">
                        <div class="col-md-6 mb-3">
                            <label class="gold-label">VALUATION [cite: 86]</label>
                            <input type="number" name="price" class="form-control" value="<%= product.price %>" required> [cite: 87]
                        </div>
                        <div class="col-md-6 mb-3">
                            <label class="gold-label">STOCK [cite: 88]</label>
                            <input type="number" name="quantity" class="form-control" value="<%= product.quantity %>" required> [cite: 88]
                        </div>
                    </div>

                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-gold py-2">UPDATE COLLECTION [cite: 91]</button>
                        <a href="/" class="btn btn-outline-secondary py-2">Discard Changes [cite: 91]</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
</body>
</html>