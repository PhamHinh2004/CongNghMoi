<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Premium Login | Inventory System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --gold: #D4AF37;
            --dark-bg: #0A0A0A;
            --card-surface: #1C1C1E;
            --border-color: rgba(212, 175, 55, 0.3);
        }

        body {
            background-color: var(--dark-bg);
            color: #F5F5F7;
            font-family: 'Plus Jakarta Sans', sans-serif;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
        }

        .login-card {
            background: var(--card-surface);
            border: 1px solid var(--border-color);
            padding: 3rem;
            border-radius: 2px; /* Đồng bộ góc vuông nhẹ của Index */
            width: 100%;
            max-width: 450px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.4);
        }

        .gold-label {
            color: var(--gold);
            text-transform: uppercase;
            letter-spacing: 2px;
            font-size: 0.75rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
            display: block;
        }

        .form-control {
            background: transparent;
            border: none;
            border-bottom: 1px solid var(--border-color);
            border-radius: 0;
            color: white;
            padding: 0.75rem 0;
            margin-bottom: 2rem;
        }

        .form-control:focus {
            background: transparent;
            color: white;
            border-color: var(--gold);
            box-shadow: none;
        }

        .btn-premium {
            background: var(--gold);
            color: black;
            border: none;
            border-radius: 0;
            padding: 12px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            width: 100%;
            transition: all 0.3s ease;
        }

        .btn-premium:hover {
            background: white;
            transform: translateY(-2px);
        }

        .header-text {
            text-align: center;
            margin-bottom: 3rem;
        }

        .header-text h2 {
            font-weight: 300;
            letter-spacing: 4px;
            text-transform: uppercase;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>

<div class="login-card">
    <div class="header-text">
        <span class="gold-label">Access Portal</span>
        <h2>AUTHENTICATION</h2>
    </div>

    <form action="/login" method="POST">
        <label class="gold-label">Username</label>
        <input type="text" name="username" class="form-control" placeholder="Enter your identification" required>

        <label class="gold-label">Password</label>
        <input type="password" name="password" class="form-control" placeholder="••••••••" required>

        <button type="submit" class="btn-premium">Sign In</button>
    </form>

    <div class="mt-4 text-center">
        <small style="color: rgba(255,255,255,0.3)">Premium Inventory Management v4.0</small>
    </div>
</div>

</body>
</html>