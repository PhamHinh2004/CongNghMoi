<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <title>New Product Selection</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #0f0f0f; color: #fff; }
        .card { background-color: #1a1a1a; border: 1px solid #d4af37; border-radius: 20px; }
        .form-label { color: #d4af37; font-weight: 500; font-size: 0.9rem; }
        .form-control { background-color: #262626; border: 1px solid #333; color: #fff; padding: 12px; border-radius: 10px; }
        .form-control:focus { background-color: #333; border-color: #d4af37; box-shadow: 0 0 10px rgba(212, 175, 55, 0.2); color: #fff; }
        .btn-gold { background-color: #d4af37; color: #000; font-weight: bold; border-radius: 10px; padding: 12px; transition: 0.3s; }
        .btn-gold:hover { background-color: #b8962e; transform: scale(1.02); }
    </style>
</head>
<body>
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-5">
            <div class="card shadow p-4">
                <h3 class="text-center mb-4" style="color: #d4af37; letter-spacing: 2px;">ADD NEW ITEM [cite: 73]</h3>
                <form action="/add" method="POST" enctype="multipart/form-data"> [cite: 73]
                    <div class="mb-3">
                        <label class="form-label">PRODUCT NAME [cite: 74]</label>
                        <input type="text" name="name" class="form-control" required> [cite: 74]
                    </div>
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label class="form-label">PRICE (VNĐ) [cite: 75]</label>
                            <input type="number" name="price" class="form-control" required> [cite: 76]
                        </div>
                        <div class="col-md-6 mb-3">
                            <label class="form-label">STOCK [cite: 77]</label>
                            <input type="number" name="quantity" class="form-control" required> [cite: 77]
                        </div>
                    </div>
                    <div class="mb-4">
                        <label class="form-label">REPRESENTATIVE IMAGE [cite: 78]</label>
                        <input type="file" name="image" class="form-control" accept="image/*" required> [cite: 78]
                    </div>
                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-gold">CONFIRM ENTRY [cite: 79]</button>
                        <a href="/" class="btn btn-link text-muted text-decoration-none small text-center">Discard and Return [cite: 80]</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
</body>
</html>