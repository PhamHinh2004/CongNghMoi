<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Quản lý Sản Phẩm</title>
    <!-- Thêm chút CSS cho dễ nhìn (Optional) -->
    <style>
        body { font-family: sans-serif; padding: 20px; }
        table { border-collapse: collapse; width: 100%; margin-top: 20px; }
        th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        th { background-color: #f2f2f2; }
        form { background: #f9f9f9; padding: 15px; border-radius: 5px; }
        input { margin: 5px 0; padding: 5px; }
        button { cursor: pointer; background: green; color: white; border: none; padding: 8px 15px; }
    </style>
</head>
<body>

    <h1>Demo Node.js + MySQL (Docker)</h1>

    <!-- FORM THÊM SẢN PHẨM -->
    <div style="width: 300px;">
        <h3>Thêm sản phẩm mới</h3>
        <form action="/add" method="post">
            <label>Tên SP:</label><br>
            <input type="text" name="name" required><br>
            
            <label>Giá:</label><br>
            <input type="number" name="price" required><br>
            
            <label>Số lượng:</label><br>
            <input type="number" name="quantity" required><br>
            <br>
            <button type="submit">Lưu vào DB</button>
        </form>
    </div>

    <!-- DANH SÁCH SẢN PHẨM -->
    <h3>Danh sách hiện có</h3>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Tên sản phẩm</th>
                <th>Giá</th>
                <th>Số lượng</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <% if (products.length === 0) { %>
                <tr><td colspan="4">Chưa có dữ liệu</td></tr>
            <% } %>

            <!-- Vòng lặp EJS để hiển thị dữ liệu -->
            <% products.forEach(p => { %>
                <tr>
                    <td><%= p.id %></td>
                    <td><%= p.name %></td>
                    <td><%= p.price %></td>
                    <td><%= p.quantity %></td>
                    <td>
                        <form action="/delete/<%= p.id %>" method="post" style="display:inline;">
                            <button type="submit" style="background:red;">Xóa</button>
                        </form>
                        <form action="/edit/<%= p.id %>" method="get" style="display:inline;">
                            <button type="submit" style="background:orange;">Sửa</button>
                        </form>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>

</body>
</html>